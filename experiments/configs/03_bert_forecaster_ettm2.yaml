name: bert_forecaster_ettm2_pred96
description: Text-only BERT forecaster on ETTm2, pred_len=96 (GPU required)
# GPU REQUIRED — connect Colab GPU runtime via VSCode extension before running

model:
  name: BERTForecaster
  task_name: long_term_forecast
  enc_in: 7
  seq_len: 512
  label_len: 48
  pred_len: 96
  text_hidden: 768   # BERT hidden size (fixed)
  dropout: 0.1
  # unused fields kept for namespace compatibility
  d_model: 128
  n_heads: 8
  e_layers: 2
  d_layers: 1
  d_ff: 768
  factor: 1
  activation: gelu
  moving_avg: 25
  individual: false

data:
  dataset: ETTm2
  root_path: ./dataset/ETT-small/
  data_path: ETTm2.csv
  features: M
  target: OT
  freq: t

training:
  train_epochs: 15
  batch_size: 32
  learning_rate: 0.001
  patience: 5
  seed: 2024

compute:
  gpu: 0
  num_workers: 4
  use_amp: false
