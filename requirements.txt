# Core
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.23.0
pandas>=1.5.0
scikit-learn>=1.2.0
PyYAML>=6.0

# Required by layers/
einops>=0.6.0

# Required by BERT-based fusion models (03–06)
# torch>=2.4 is satisfied by current envs, so no upper bound needed
transformers>=4.30.0

# Data / utilities
scipy>=1.10.0
tqdm>=4.64.0
matplotlib>=3.7.0

# Optional: only needed if running Reformer model
# reformer-pytorch==1.4.4
